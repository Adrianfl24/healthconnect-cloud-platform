apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthconnect-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthconnect-service
                port:
                  number: 8081
